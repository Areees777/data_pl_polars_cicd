name: release-dev-env

on:
  pull_request:
    branches:
      - dev
    types:
      - closed
  
jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v4

      - name: Check secret
        run: |
          echo "API_KEY_DEV is set"
          test -n "$API_KEY_DEV"
        env:
          API_KEY_DEV: ${{ secrets.API_KEY_DEV }}

      - name: release-dev-env
        run: |
          echo "Aquí va tu deploy real"
